# Handoff — RESOLVER-V3-048 Protocol-v4 Phase A post-merge remediation (2026-07-27)

1. **Task ID/status:** RESOLVER-V3-048 — `in_progress — Protocol-v4 executable zero-call preflight complete; live Development not authorized`; basis `dd81439a36ac4122cce5d3bdeeb2562ce84271ac` (PR #190 merge, verified as the actual remote tip of `chore/clean-arch-structure` at start).
2. **What changed:** An independent post-merge review of PR #190 found 12 reproducible defects: a fake "22-scenario dry run" (name-length assertion only); no telemetry/ledger wiring; a pricing-version literal diverging from the real pricing table; a self-labelled (content-blind) evaluator hash; boolean-only Development checkpoint/evaluation gates; a freely-constructible candidate-selection record; a Holdout-authorization gate that never checked its own `maxCalls`/`maxTokens`/`maxCostUsd` against the plan; a single-stage plan enumerating all three candidates under Holdout while deriving the Holdout budget via a hard-coded `candidateId === 'H0'` filter; and terminal/count validators that ignored most of their own fields. Replaced with: a single pricing authority (`ResolverV3048ProtocolV4Pricing.ts`) resolved from the same table `LiveProviderBudgetGate` reserves against; a real evaluator-manifest hash (`ResolverV3048ProtocolV4EvaluatorHash.ts`) over actual `RepresentativeHybridV1LiveMetrics.ts`/`RepresentativeHybridV1LiveReportBuilder.ts` content, reusing the existing V3-039 execution-tree-hash algorithm; a two-stage Master Protocol Plan / Holdout Execution Plan contract where the Master Plan's Holdout template carries no candidate; artifact-hash-bound Development evidence and candidate selection (`selectCandidate()` no longer reachable except through validated, sealed artifacts); a strict Human/Fake Holdout-authorization gate; closed `MeasuredCount`/terminal-metadata validators; real telemetry/ledger wrapper wiring (`ResolverV3048ProtocolV4Telemetry.ts`) reusing V3-039's wall-clock wrapper; and a genuinely executable 22-scenario dry-run engine (`ResolverV3048ProtocolV4DryRun.ts`) that drives the real `runVariantCCase()`/candidate-dependent interpreter against fake transport/sources.
3. **Why it changed:** PR #190's Phase A claimed "zero-call preflight complete" without ever executing a scenario, wiring telemetry/ledger, or closing the pricing/hash/authorization/validator gaps an independent review found — the claim was premature per this repository's own "no fake completion claims" rule.
4. **Files changed:** new `src/features/nutrition/benchmark/protocolV4/ResolverV3048ProtocolV4Pricing.ts`, `ResolverV3048ProtocolV4EvaluatorHash.ts`, `ResolverV3048ProtocolV4Telemetry.ts`, `ResolverV3048ProtocolV4DryRun.ts`; rewritten `ResolverV3048ProtocolV4.ts`; new/rewritten tests under `protocolV4/__tests__/` (`ResolverV3048ProtocolV4.test.ts`, `ResolverV3048ProtocolV4RedBaseline.test.ts`, `ResolverV3048ProtocolV4DryRun.test.ts`); `reports/RESOLVER_V3_048_PROTOCOL_V4_PHASE_A_PREFLIGHT.md` (additive post-merge correction section); `ROADMAP.md`; this handoff. No V3-039 evidence, corpus, ground truth, evaluator logic, BLS artifact, CI, dependency, or production file changed.
5. **Verification executed:** a failing-baseline test file run unmodified against the merge commit (10/10 passed, proving all 16 listed defects real) before any implementation edit; full protocolV4 suite (3 suites/63 tests); full benchmark-directory suite (73 suites/808 tests, including V3-039 compatibility and V3-042/043-051 regressions); repo-wide `npm run typecheck`, `npm run lint`, `npm run format:check`; `git diff --check`.
6. **Verification result:** All of the above passed. `npm run verify`'s full-repo `npm test` stage has the same known non-termination symptom already documented in prior V3-047/048 handoffs for this repository; typecheck/lint/format/diff and every individually-run Jest suite (including the entire `src/features/nutrition/benchmark` directory) passed cleanly. Provider calls 0; provider cost USD 0; no credential read (`ANTHROPIC_API_KEY` is a literal placeholder string, never read from `process.env`, in every dry-run fixture).
7. **Known issues/blockers/residual risks:** Green GitHub Verify (the canonical full-suite run) is still required before merge, per this repository's own prior-task convention for this exact symptom. No live quality/cost/reliability evidence exists or was claimed — this remains exclusively Phase B's job, still requiring separate explicit human authorization. The 352-call / USD 5.586944 proposal-only budget total is numerically unchanged from PR #190 (only its derivation path was corrected) and remains explicitly **not authorized**. G2 remains **not passed**; V3-010 remains `blocked`.
8. **Human review/next steps:** Review the post-merge correction section of the Phase-A report and this diff; require green GitHub Verify before merge. Phase B still requires explicit human approval of exact call/token/USD limits and must never auto-continue from Development to Holdout.
