# Handoff — RESOLVER-V3-048 Final Evidence-Lineage remediation (2026-07-28)

1. **Task ID/status:** RESOLVER-V3-048 — `in_progress — Protocol-v4 executable zero-call preflight complete; live Development not authorized`; basis `7a5f6b53102910c495147d346348bce6c3bc4d14` (verified tip of `chore/clean-arch-structure` at start; contains PR #191's merge `f8ba324fed21675389c25d8e2b3480ecb4fb3855` as an ancestor, satisfying the task's required minimum).
2. **What changed:** An independent post-PR-#191 review found 18 reproducible evidence-lineage defects (full list in the ROADMAP entry and the report's new post-merge section): terminal metadata still reconstructed/normalized after the fact (`unknown`→`estimated`, status guessed from `failureKind`, latency defaulted to `0`, a fabricated reservation ID); the real 22-scenario run never fed `buildSyntheticDevelopmentEvidence()`, which itself sealed empty telemetry/ledger/raw-results with hard-coded "all G2 passed" evaluations; `allMandatoryG2CriteriaPass` could be `true` alongside `not_evaluable`/`requires_human_judgment`; Candidate Selection Records were never re-validated against a recomputed winner; telemetry and ledger shared the same object instance (tautological parity check); no exactly-once lock existed per `callId`; the wall-clock wrapper only completed the timeout path itself; Holdout Plan/Authorization validation checked only self-consistency, never full re-derivation or per-field token/concurrency limits; `artifactTargetUnused`/`consumed` were unverified booleans; no Development Authorization Record/gate existed; Masterplan revalidation never recomputed non-pricing identities; `missing_text_block` was wrongly classified as an unbillable network failure; and the dry run round-tripped only the Category artifact. All 18 are closed on this branch. New modules: `ResolverV3048ProtocolV4AttemptContext.ts` (immutable, pre-dispatch-frozen attempt context), `ResolverV3048ProtocolV4Reservation.ts` (real budget-reservation evidence wrapper around the unmodified V3-013 gate), `ResolverV3048ProtocolV4CallStateMachine.ts` (`planned→authorized→reserved→dispatched→terminal`, one-shot transitions), `ResolverV3048ProtocolV4AttemptWrapper.ts` (single authoritative all-path attempt wrapper — no outside metadata reconstruction), `ResolverV3048ProtocolV4Evaluation.ts` (evaluation derivation adapter + G2 coherence), `ResolverV3048ProtocolV4DevelopmentAuthorization.ts` (Development-phase authorization gate symmetric to Holdout's), `ResolverV3048ProtocolV4ArtifactStore.ts` (atomic filesystem-backed artifact store, dry-run-root-restricted), `ResolverV3048ProtocolV4DevelopmentRunner.ts` (real zero-network Development execution across all 3 candidates), `ResolverV3048ProtocolV4Fixtures.ts` (shared fake transport/source fixtures). Rewrote `ResolverV3048ProtocolV4.ts` (G2 coherence, selection-winner recomputation, full Masterplan/Holdout-plan re-derivation, Holdout-authorization token/concurrency checks, `missing_text_block` taxonomy fix) and `ResolverV3048ProtocolV4Telemetry.ts` (exactly-once + independently-cloned records) and `ResolverV3048ProtocolV4DryRun.ts` (rewired to the new wrapper; fault matrix grew 22→27; added `runProtocolV4MiniProtocolRun()`).
3. **Why it changed:** The task's own independent review (and direct inspection of the actual merged code) confirmed every one of the 18 listed defects at specific line numbers before any fix was written; PR #191's "Phase A executable preflight complete" claim was still premature for evidence-lineage purposes even though it correctly fixed the prior PR #190 defects.
4. **Files changed:** 9 new files under `src/features/nutrition/benchmark/protocolV4/` (listed above); rewritten `ResolverV3048ProtocolV4.ts`, `ResolverV3048ProtocolV4Telemetry.ts`, `ResolverV3048ProtocolV4DryRun.ts`; new test `ResolverV3048ProtocolV4FinalEvidenceLineageRedBaseline.test.ts` (25 focused items); updated `ResolverV3048ProtocolV4.test.ts`, `ResolverV3048ProtocolV4DryRun.test.ts`, `ResolverV3048ProtocolV4RedBaseline.test.ts` (non-empty telemetry/ledger fixtures, new signatures); `.gitignore` (added `tmp/resolver-v3-048-protocol-v4-dry-run/`, the dry-run artifact store's scratch output — never canonical evidence); `reports/RESOLVER_V3_048_PROTOCOL_V4_PHASE_A_PREFLIGHT.md` (additive new post-merge section); `ROADMAP.md`; this handoff. No V3-039 evidence, corpus, ground truth, evaluator logic, BLS artifact, CI, dependency, or production file changed.
5. **Verification executed:** `npm run typecheck`, `npm run lint`, `npm run format:check` (repo-wide); `npx jest --runInBand src/features/nutrition/benchmark/protocolV4` (4 suites / 92 tests); `npx jest --runInBand src/features/nutrition/benchmark` (74 suites / 837 tests — the full V3-039-compatibility/V3-042-evaluator/V3-043..051-regression sweep); `git status`/`git diff --check`.
6. **Verification result:** All of the above passed. The new `runProtocolV4MiniProtocolRun()` genuinely executes all 324 planned Development observations (fake, zero-network) across H0/H1/H2 in ~1.5s, derives evaluations, selects a candidate, derives and re-validates a Holdout Execution Plan, and exercises (but never consumes into a live run) the Holdout gate. Provider calls 0; provider cost USD 0; no credential read (`ANTHROPIC_API_KEY` is a literal placeholder string, never read from `process.env`, anywhere in this task's fixtures).
7. **Known issues/blockers/residual risks:** Green GitHub Verify is still required before merge. The evaluation-derivation adapter (Teil 8) is a genuinely artifact-derived, deterministic pipeline but does NOT re-invoke `RepresentativeHybridV1LiveMetrics`'s internal G2-A..G2-G dimension functions directly (documented as a deliberate scope decision in `ResolverV3048ProtocolV4Evaluation.ts`'s docstring, to avoid risking a "fachliche Änderung des korrigierten G2-Evaluators" the task's hard limits forbid) — every gate verdict is still genuinely derived from real executed artifacts via zero/nonzero structural criteria, not a free literal, but it is a benchmark-local derivation rather than a direct call into the pinned evaluator's own dimension code. The 352-call / USD 5.586944 proposal-only budget remains numerically unchanged and explicitly **not authorized**; no `human_live` authorization was created; no Development or Holdout live execution occurred. G2 remains **not passed**; V3-010 remains `blocked`; V3-047 remains `done`.
8. **Human review/next steps:** Review this diff and the report's new post-merge section; require green GitHub Verify before merge. Phase B (live execution) still requires a separate, explicit human authorization of exact call/token/USD limits and must never auto-continue from Development to Holdout.
