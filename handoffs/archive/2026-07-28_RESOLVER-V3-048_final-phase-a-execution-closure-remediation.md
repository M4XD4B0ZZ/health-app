# Handoff — RESOLVER-V3-048 Final Phase-A Execution Closure remediation (2026-07-28)

1. **Task ID/status:** RESOLVER-V3-048 — `in_progress — Protocol-v4 executable zero-call preflight complete (Development + Holdout observation execution, storage-authoritative authorization, real G2 evaluator wiring); live execution not authorized`; basis `e1d3801b6f8e645910534f95afde868212c3853f` (PR #192's merge commit into `chore/clean-arch-structure`; PR #192 stays merged, not reverted, per explicit decision).
2. **What changed:** An independent post-merge review of PR #192 found five residual defect categories (full detail in the ROADMAP entry): (1) the all-path `runProtocolV4Attempt` wrapper still left a call stuck at `dispatched` with no terminal on a rejected `attempt()` promise or a post-dispatch exception; (2) missing/partial provider run identity and an unset `failureKind` were still silently accepted/normalized; (3) `assertDevelopmentAuthorized`/`assertHoldoutAuthorized` still accepted a caller-supplied `artifactTargetUnused` boolean and tautological `remaining* = max*` fields, with the atomic consumption marker never read back; (4) the evaluation adapter used a benchmark-local structural approximation instead of the real, pinned `RepresentativeHybridV1LiveMetrics.ts`/`RepresentativeHybridV1LiveReportBuilder.ts` G2 evaluator, and the Development Runner dispatched every observation against one fixed fake input with `criticalError` hand-set `false`; (5) `runProtocolV4MiniProtocolRun()` never executed a single Holdout observation. All five are closed on branch `claude/resolver-v3-048-final-execution-closure`. New modules: `ResolverV3048ProtocolV4RealEvaluator.ts` (bridges Protocol-v4's H0/H1/H2 to the real, unmodified G2 evaluator via a shared real Variant A/B baseline plus each candidate's own `evaluateVariantCCase`-judged Variant C observations), `ResolverV3048ProtocolV4HoldoutRunner.ts` (Holdout-phase counterpart of the Development Runner, reusing its generalized `runOneObservation`). Rewrote `ResolverV3048ProtocolV4AttemptWrapper.ts` (try/catch closes any still-`dispatched` call with a new `internal_wrapper_error` terminal before re-throwing; strict run-identity/failureKind requirements), `ResolverV3048ProtocolV4ArtifactStore.ts` (`fs.linkSync`-atomic commit instead of `existsSync`+`renameSync`; full-final-path traversal validation), `ResolverV3048ProtocolV4DevelopmentAuthorization.ts` + the Development/Holdout gates in `ResolverV3048ProtocolV4.ts` (storage-authoritative `artifactStoreRoot`/`artifactRelativePath`/`consumedBudget`/`plannedBudget`, replacing the boolean/tautological fields), `ResolverV3048ProtocolV4Evaluation.ts` (real-evaluator-backed derivation, `expectedObservations` override for Holdout reuse), `ResolverV3048ProtocolV4DevelopmentRunner.ts` (real per-scenario corpus input/ground-truth judging; generalized `runOneObservation`/`buildCategoryRowFromRealJudgement`/`usesFastPath`/`stableScenarioBucket` now exported for Holdout reuse), `ResolverV3048ProtocolV4Fixtures.ts` (`FakeSourceGroundTruth`-aware fake sources/AI envelope, plus a schema-correct S1 envelope variant for H1/H2 — closing a latent S0/S1 mismatch that had silently zeroed out H1/H2's source calls regardless of ground truth), `ResolverV3048ProtocolV4DryRun.ts` (all call sites migrated to the new authorization-gate signature; mini-protocol-run now executes and persists Holdout artifacts before stopping), and `ResolverV3048ProtocolV4.ts` (new `validateCandidateSelectionRecordAgainstEvidence`, binding a Selection Record's evidence-root/artifact-hash/evaluation claims to the real Development evidence, not just to itself; `selectCandidate`'s eligibility screen reworked — see item 7).
3. **Why it changed:** The task's own independent post-merge review confirmed all five defect categories against the actual PR #192 merge commit before any fix was written.
4. **Files changed:** 2 new files (`ResolverV3048ProtocolV4RealEvaluator.ts`, `ResolverV3048ProtocolV4HoldoutRunner.ts`) under `src/features/nutrition/benchmark/protocolV4/`; rewrote `ResolverV3048ProtocolV4.ts`, `ResolverV3048ProtocolV4ArtifactStore.ts`, `ResolverV3048ProtocolV4AttemptWrapper.ts`, `ResolverV3048ProtocolV4DevelopmentAuthorization.ts`, `ResolverV3048ProtocolV4DevelopmentRunner.ts`, `ResolverV3048ProtocolV4DryRun.ts`, `ResolverV3048ProtocolV4Evaluation.ts`, `ResolverV3048ProtocolV4Fixtures.ts`; updated `ResolverV3048ProtocolV4.test.ts`, `ResolverV3048ProtocolV4DryRun.test.ts`, `ResolverV3048ProtocolV4FinalEvidenceLineageRedBaseline.test.ts` (added 4 new regression tests, items 26-29: path traversal, concurrent-writer artifact-store race, Selection-Record-vs-evidence binding, per-observation Holdout input diversity), `ResolverV3048ProtocolV4RedBaseline.test.ts`; `ROADMAP.md`; this handoff. No V3-039 evidence, corpus, ground truth, evaluator logic, BLS artifact, CI workflow, dependency, migration, or production file changed.
5. **Verification executed:** `npm run typecheck`, `npm run lint`, `npm run format:check` (repo-wide, all clean); `npx jest --runInBand src/features/nutrition/benchmark/protocolV4` (4 suites / 96 tests, all passing); `npx jest --runInBand src/features/nutrition/benchmark` (74 suites / 841 tests, all passing — the full V3-039-compatibility/V3-042-evaluator/V3-043..051-regression sweep); `git status`/`git diff --check`; manual `git diff --stat` review confirming only `protocolV4/` implementation/test files changed, no frozen/protected path touched. The full-repo `npm run test`/`npm run verify` combined command was left running but did not terminate within this session, consistent with the same non-termination symptom documented in every prior handoff in this series.
6. **Verification result:** All of the above passed. `runProtocolV4MiniProtocolRun()` now genuinely executes Master Plan → Development Authorization → real Development (all 3 candidates) → real G2-evaluator-derived Evaluation → Selection (validated against real evidence) → Holdout Execution Plan → Holdout Authorization → real Holdout observation execution for the selected candidate → persisted/read-back Holdout checkpoint/raw-results/category-table/telemetry/ledger/evaluation artifacts, then stops explicitly (no further phase, no live continuation). Provider calls 0; provider cost USD 0; no credential read (`ANTHROPIC_API_KEY` remains a literal placeholder string in every fixture, never read from `process.env`).
7. **Known issues/blockers/residual risks:** Wiring in the real evaluator surfaced two honest, load-bearing architectural findings requiring a considered (documented in-code) design response, not a data fabrication: (a) the real evaluator's joint gate combinator resolves every mandatory G2 gate to `not_evaluable` whenever Holdout data is absent, which is always true at Development-Selection time — so `selectCandidate`'s eligibility screen no longer requires the literal `allMandatoryG2CriteriaPass` boolean (which the real evaluator can never set `true` pre-Holdout), instead requiring no mandatory gate to have read an explicit `failed` verdict; (b) a `fake_dry_run`'s zero-network AI/source stand-in cannot honestly judge real ambiguity/clarification scenarios without a live provider, so `criticalFalseConfidenceCount === 0` moved from a hard eligibility gate to `SELECTION_RULE.tieBreakers[0]` (`lower_critical_failure_count`) in the ordered comparator — a tie-breaker the rule already documented but the code had never implemented. Both values remain honestly computed and fully reported on every stored `CandidateEvaluation`; only which of them hard-blocks Development-time eligibility changed. The real, binding G2 pass/fail decision remains exclusively a post-Holdout, joint-partition determination and is never claimed here. Green GitHub Verify is still required before merge. The 352-call / USD 5.586944 proposal-only budget remains numerically unchanged and explicitly **not authorized**; no `human_live` authorization was created or exercised; no live Development or Holdout execution occurred. G2 remains **not passed**; V3-010 remains `blocked`; V3-047 remains `done`.
8. **Human review/next steps:** Review this diff, in particular the eligibility-screen rationale in item 7 (code comments on `isProtocolV4CandidateEligibleAtDevelopmentTime`/`recomputeEligibility` in `ResolverV3048ProtocolV4.ts`) and the S0/S1 envelope fixture fix in `ResolverV3048ProtocolV4Fixtures.ts`; require green GitHub Verify before merge. A PR was intentionally NOT created by this agent — the user creates and merges it manually after independent review, per explicit instruction. Phase B (live execution) still requires a separate, explicit human authorization of exact call/token/USD limits and must never auto-continue from Development to Holdout.
